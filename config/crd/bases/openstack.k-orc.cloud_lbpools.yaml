---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: lbpools.openstack.k-orc.cloud
spec:
  group: openstack.k-orc.cloud
  names:
    categories:
    - openstack
    kind: LBPool
    listKind: LBPoolList
    plural: lbpools
    singular: lbpool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Resource ID
      jsonPath: .status.id
      name: ID
      type: string
    - description: Availability status of resource
      jsonPath: .status.conditions[?(@.type=='Available')].status
      name: Available
      type: string
    - description: Message describing current progress status
      jsonPath: .status.conditions[?(@.type=='Progressing')].message
      name: Message
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LBPool is the Schema for an ORC resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec specifies the desired state of the resource.
            properties:
              cloudCredentialsRef:
                description: cloudCredentialsRef points to a secret containing OpenStack
                  credentials
                properties:
                  cloudName:
                    description: cloudName specifies the name of the entry in the
                      clouds.yaml file to use.
                    maxLength: 256
                    minLength: 1
                    type: string
                  secretName:
                    description: |-
                      secretName is the name of a secret in the same namespace as the resource being provisioned.
                      The secret must contain a key named `clouds.yaml` which contains an OpenStack clouds.yaml file.
                      The secret may optionally contain a key named `cacert` containing a PEM-encoded CA certificate.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - cloudName
                - secretName
                type: object
              import:
                description: |-
                  import refers to an existing OpenStack resource which will be imported instead of
                  creating a new one.
                maxProperties: 1
                minProperties: 1
                properties:
                  filter:
                    description: |-
                      filter contains a resource query which is expected to return a single
                      result. The controller will continue to retry if filter returns no
                      results. If filter returns multiple results the controller will set an
                      error state and will not continue to retry.
                    minProperties: 1
                    properties:
                      description:
                        description: description of the existing resource
                        maxLength: 255
                        minLength: 1
                        type: string
                      lbAlgorithm:
                        description: lbAlgorithm filters by the load balancing algorithm.
                        enum:
                        - LEAST_CONNECTIONS
                        - ROUND_ROBIN
                        - SOURCE_IP
                        - SOURCE_IP_PORT
                        type: string
                      listenerRef:
                        description: listenerRef filters by the Listener this pool
                          is associated with.
                        maxLength: 253
                        minLength: 1
                        type: string
                      loadBalancerRef:
                        description: loadBalancerRef filters by the LoadBalancer this
                          pool is associated with.
                        maxLength: 253
                        minLength: 1
                        type: string
                      name:
                        description: name of the existing resource
                        maxLength: 255
                        minLength: 1
                        pattern: ^[^,]+$
                        type: string
                      notTags:
                        description: |-
                          notTags is a list of tags to filter by. If specified, resources which
                          contain all of the given tags will be excluded from the result.
                        items:
                          maxLength: 255
                          minLength: 1
                          type: string
                        maxItems: 64
                        type: array
                        x-kubernetes-list-type: set
                      notTagsAny:
                        description: |-
                          notTagsAny is a list of tags to filter by. If specified, resources
                          which contain any of the given tags will be excluded from the result.
                        items:
                          maxLength: 255
                          minLength: 1
                          type: string
                        maxItems: 64
                        type: array
                        x-kubernetes-list-type: set
                      projectRef:
                        description: projectRef filters by the Project this pool is
                          associated with.
                        maxLength: 253
                        minLength: 1
                        type: string
                      protocol:
                        description: protocol filters by the protocol used by the
                          pool.
                        enum:
                        - HTTP
                        - HTTPS
                        - PROXY
                        - PROXYV2
                        - SCTP
                        - TCP
                        - UDP
                        type: string
                      tags:
                        description: |-
                          tags is a list of tags to filter by. If specified, the resource must
                          have all of the tags specified to be included in the result.
                        items:
                          maxLength: 255
                          minLength: 1
                          type: string
                        maxItems: 64
                        type: array
                        x-kubernetes-list-type: set
                      tagsAny:
                        description: |-
                          tagsAny is a list of tags to filter by. If specified, the resource
                          must have at least one of the tags specified to be included in the
                          result.
                        items:
                          maxLength: 255
                          minLength: 1
                          type: string
                        maxItems: 64
                        type: array
                        x-kubernetes-list-type: set
                    type: object
                  id:
                    description: |-
                      id contains the unique identifier of an existing OpenStack resource. Note
                      that when specifying an import by ID, the resource MUST already exist.
                      The ORC object will enter an error state if the resource does not exist.
                    format: uuid
                    type: string
                type: object
              managedOptions:
                description: managedOptions specifies options which may be applied
                  to managed objects.
                properties:
                  onDelete:
                    default: delete
                    description: |-
                      onDelete specifies the behaviour of the controller when the ORC
                      object is deleted. Options are `delete` - delete the OpenStack resource;
                      `detach` - do not delete the OpenStack resource. If not specified, the
                      default is `delete`.
                    enum:
                    - delete
                    - detach
                    type: string
                type: object
              managementPolicy:
                default: managed
                description: |-
                  managementPolicy defines how ORC will treat the object. Valid values are
                  `managed`: ORC will create, update, and delete the resource; `unmanaged`:
                  ORC will import an existing resource, and will not apply updates to it or
                  delete it.
                enum:
                - managed
                - unmanaged
                type: string
                x-kubernetes-validations:
                - message: managementPolicy is immutable
                  rule: self == oldSelf
              resource:
                description: |-
                  resource specifies the desired state of the resource.

                  resource may not be specified if the management policy is `unmanaged`.

                  resource must be specified if the management policy is `managed`.
                properties:
                  adminStateUp:
                    description: |-
                      adminStateUp is the administrative state of the pool, which is up (true)
                      or down (false).
                    type: boolean
                  alpnProtocols:
                    description: |-
                      alpnProtocols is a list of ALPN protocols for backend TLS connections.
                      Available protocols: h2, http/1.0, http/1.1.
                    items:
                      maxLength: 32
                      type: string
                    maxItems: 10
                    type: array
                    x-kubernetes-list-type: set
                  caTLSContainerRef:
                    description: |-
                      caTLSContainerRef is a reference to a secret containing the CA
                      certificate for backend re-encryption.
                    maxLength: 255
                    type: string
                    x-kubernetes-validations:
                    - message: caTLSContainerRef is immutable
                      rule: self == oldSelf
                  crlContainerRef:
                    description: |-
                      crlContainerRef is a reference to a secret containing the CA
                      revocation list for backend re-encryption.
                    maxLength: 255
                    type: string
                    x-kubernetes-validations:
                    - message: crlContainerRef is immutable
                      rule: self == oldSelf
                  description:
                    description: description is a human-readable description for the
                      resource.
                    maxLength: 255
                    minLength: 1
                    type: string
                  lbAlgorithm:
                    description: |-
                      lbAlgorithm is the load balancing algorithm used to distribute traffic
                      to the pool's members.
                    enum:
                    - LEAST_CONNECTIONS
                    - ROUND_ROBIN
                    - SOURCE_IP
                    - SOURCE_IP_PORT
                    type: string
                    x-kubernetes-validations:
                    - message: lbAlgorithm is immutable
                      rule: self == oldSelf
                  listenerRef:
                    description: |-
                      listenerRef is a reference to the ORC Listener which this pool is
                      associated with as the default pool. Either loadBalancerRef or listenerRef
                      must be specified.
                    maxLength: 253
                    minLength: 1
                    type: string
                    x-kubernetes-validations:
                    - message: listenerRef is immutable
                      rule: self == oldSelf
                  loadBalancerRef:
                    description: |-
                      loadBalancerRef is a reference to the ORC LoadBalancer which this pool
                      is associated with. Either loadBalancerRef or listenerRef must be specified.
                    maxLength: 253
                    minLength: 1
                    type: string
                    x-kubernetes-validations:
                    - message: loadBalancerRef is immutable
                      rule: self == oldSelf
                  members:
                    description: members is a list of backend members for this pool.
                    items:
                      description: LBPoolMemberSpec defines a member of an LB pool.
                      properties:
                        address:
                          description: address is the IP address of the member to
                            receive traffic.
                          maxLength: 45
                          minLength: 1
                          type: string
                        adminStateUp:
                          description: adminStateUp is the administrative state of
                            the member (up=true, down=false).
                          type: boolean
                        backup:
                          description: |-
                            backup indicates whether this is a backup member. Backup members only
                            receive traffic when all non-backup members are down.
                          type: boolean
                        name:
                          description: name is a human-readable name for the member.
                          maxLength: 255
                          type: string
                        protocolPort:
                          description: protocolPort is the port on which the member
                            application is listening.
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                        subnetRef:
                          description: subnetRef is a reference to the ORC Subnet
                            where the member resides.
                          maxLength: 253
                          minLength: 1
                          type: string
                        weight:
                          description: |-
                            weight is the relative portion of traffic this member should receive.
                            A member with weight 10 receives 5x the traffic of a member with weight 2.
                            Default is 1.
                          format: int32
                          maximum: 256
                          minimum: 0
                          type: integer
                      required:
                      - address
                      - protocolPort
                      type: object
                    maxItems: 256
                    type: array
                    x-kubernetes-list-type: atomic
                  name:
                    description: |-
                      name will be the name of the created resource. If not specified, the
                      name of the ORC object will be used.
                    maxLength: 255
                    minLength: 1
                    pattern: ^[^,]+$
                    type: string
                  projectRef:
                    description: projectRef is a reference to the ORC Project which
                      this resource is associated with.
                    maxLength: 253
                    minLength: 1
                    type: string
                    x-kubernetes-validations:
                    - message: projectRef is immutable
                      rule: self == oldSelf
                  protocol:
                    description: protocol is the protocol used by the pool and its
                      members for traffic.
                    enum:
                    - HTTP
                    - HTTPS
                    - PROXY
                    - PROXYV2
                    - SCTP
                    - TCP
                    - UDP
                    type: string
                    x-kubernetes-validations:
                    - message: protocol is immutable
                      rule: self == oldSelf
                  sessionPersistence:
                    description: sessionPersistence is the session persistence configuration
                      for the pool.
                    properties:
                      cookieName:
                        description: |-
                          cookieName is the name of the cookie if persistence type is APP_COOKIE.
                          Required when type is APP_COOKIE.
                        maxLength: 255
                        type: string
                      type:
                        description: type is the type of session persistence.
                        enum:
                        - APP_COOKIE
                        - HTTP_COOKIE
                        - SOURCE_IP
                        type: string
                    required:
                    - type
                    type: object
                  tags:
                    description: tags is a list of tags which will be applied to the
                      pool.
                    items:
                      maxLength: 255
                      minLength: 1
                      type: string
                    maxItems: 64
                    type: array
                    x-kubernetes-list-type: set
                  tlsCiphers:
                    description: tlsCiphers is a colon-separated list of ciphers for
                      backend TLS connections.
                    maxLength: 2048
                    type: string
                  tlsContainerRef:
                    description: |-
                      tlsContainerRef is a reference to a secret containing a PKCS12 format
                      certificate/key bundle for backend re-encryption.
                    maxLength: 255
                    type: string
                  tlsEnabled:
                    description: |-
                      tlsEnabled enables backend re-encryption when set to true. Requires
                      TERMINATED_HTTPS or HTTPS protocol on the listener.
                    type: boolean
                    x-kubernetes-validations:
                    - message: tlsEnabled is immutable
                      rule: self == oldSelf
                  tlsVersions:
                    description: |-
                      tlsVersions is a list of TLS protocol versions to be used for backend
                      TLS connections.
                    items:
                      maxLength: 32
                      type: string
                    maxItems: 10
                    type: array
                    x-kubernetes-list-type: set
                required:
                - lbAlgorithm
                - protocol
                type: object
                x-kubernetes-validations:
                - message: either loadBalancerRef or listenerRef must be specified
                  rule: has(self.loadBalancerRef) || has(self.listenerRef)
            required:
            - cloudCredentialsRef
            type: object
            x-kubernetes-validations:
            - message: resource must be specified when policy is managed
              rule: 'self.managementPolicy == ''managed'' ? has(self.resource) : true'
            - message: import may not be specified when policy is managed
              rule: 'self.managementPolicy == ''managed'' ? !has(self.__import__)
                : true'
            - message: resource may not be specified when policy is unmanaged
              rule: 'self.managementPolicy == ''unmanaged'' ? !has(self.resource)
                : true'
            - message: import must be specified when policy is unmanaged
              rule: 'self.managementPolicy == ''unmanaged'' ? has(self.__import__)
                : true'
            - message: managedOptions may only be provided when policy is managed
              rule: 'has(self.managedOptions) ? self.managementPolicy == ''managed''
                : true'
          status:
            description: status defines the observed state of the resource.
            properties:
              conditions:
                description: |-
                  conditions represents the observed status of the object.
                  Known .status.conditions.type are: "Available", "Progressing"

                  Available represents the availability of the OpenStack resource. If it is
                  true then the resource is ready for use.

                  Progressing indicates whether the controller is still attempting to
                  reconcile the current state of the OpenStack resource to the desired
                  state. Progressing will be False either because the desired state has
                  been achieved, or because some terminal error prevents it from ever being
                  achieved and the controller is no longer attempting to reconcile. If
                  Progressing is True, an observer waiting on the resource should continue
                  to wait.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 32
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              id:
                description: id is the unique identifier of the OpenStack resource.
                type: string
              resource:
                description: resource contains the observed state of the OpenStack
                  resource.
                properties:
                  adminStateUp:
                    description: |-
                      adminStateUp is the administrative state of the pool,
                      which is up (true) or down (false).
                    type: boolean
                  alpnProtocols:
                    description: alpnProtocols is the list of ALPN protocols for backend
                      connections.
                    items:
                      maxLength: 32
                      type: string
                    maxItems: 10
                    type: array
                    x-kubernetes-list-type: atomic
                  caTLSContainerRef:
                    description: caTLSContainerRef is the reference to the CA TLS
                      container.
                    maxLength: 1024
                    type: string
                  crlContainerRef:
                    description: crlContainerRef is the reference to the CRL container.
                    maxLength: 1024
                    type: string
                  description:
                    description: description is a human-readable description for the
                      resource.
                    maxLength: 1024
                    type: string
                  healthMonitorID:
                    description: healthMonitorID is the ID of the health monitor associated
                      with this pool.
                    maxLength: 1024
                    type: string
                  lbAlgorithm:
                    description: lbAlgorithm is the load balancing algorithm used
                      by the pool.
                    maxLength: 64
                    type: string
                  listenerIDs:
                    description: listenerIDs is the list of Listener IDs this pool
                      is associated with.
                    items:
                      maxLength: 1024
                      type: string
                    maxItems: 64
                    type: array
                    x-kubernetes-list-type: atomic
                  loadBalancerIDs:
                    description: loadBalancerIDs is the list of LoadBalancer IDs this
                      pool is associated with.
                    items:
                      maxLength: 1024
                      type: string
                    maxItems: 64
                    type: array
                    x-kubernetes-list-type: atomic
                  members:
                    description: members is the list of members in this pool with
                      their details.
                    items:
                      description: LBPoolMemberStatus represents the observed state
                        of a pool member.
                      properties:
                        address:
                          description: address is the IP address of the member.
                          maxLength: 64
                          type: string
                        adminStateUp:
                          description: adminStateUp is the administrative state of
                            the member.
                          type: boolean
                        backup:
                          description: backup indicates whether this is a backup member.
                          type: boolean
                        id:
                          description: id is the unique identifier of the member in
                            OpenStack.
                          maxLength: 1024
                          type: string
                        name:
                          description: name is the human-readable name for the member.
                          maxLength: 255
                          type: string
                        operatingStatus:
                          description: operatingStatus is the operating status of
                            the member.
                          maxLength: 64
                          type: string
                        protocolPort:
                          description: protocolPort is the port on which the member
                            is listening.
                          format: int32
                          type: integer
                        provisioningStatus:
                          description: provisioningStatus is the provisioning status
                            of the member.
                          maxLength: 64
                          type: string
                        subnetID:
                          description: subnetID is the ID of the subnet the member
                            is on.
                          maxLength: 1024
                          type: string
                        weight:
                          description: weight is the weight of the member for load
                            balancing.
                          format: int32
                          type: integer
                      type: object
                    maxItems: 256
                    type: array
                    x-kubernetes-list-type: atomic
                  name:
                    description: name is a Human-readable name for the resource. Might
                      not be unique.
                    maxLength: 1024
                    type: string
                  operatingStatus:
                    description: operatingStatus is the operating status of the pool.
                    maxLength: 1024
                    type: string
                  projectID:
                    description: projectID is the ID of the Project this pool is associated
                      with.
                    maxLength: 1024
                    type: string
                  protocol:
                    description: protocol is the protocol used by the pool.
                    maxLength: 64
                    type: string
                  provisioningStatus:
                    description: provisioningStatus is the provisioning status of
                      the pool.
                    maxLength: 1024
                    type: string
                  sessionPersistence:
                    description: sessionPersistence is the session persistence configuration.
                    properties:
                      cookieName:
                        description: |-
                          cookieName is the name of the cookie if persistence type is APP_COOKIE.
                          Required when type is APP_COOKIE.
                        maxLength: 255
                        type: string
                      type:
                        description: type is the type of session persistence.
                        enum:
                        - APP_COOKIE
                        - HTTP_COOKIE
                        - SOURCE_IP
                        type: string
                    required:
                    - type
                    type: object
                  tags:
                    description: tags is the list of tags on the resource.
                    items:
                      maxLength: 255
                      type: string
                    maxItems: 64
                    type: array
                    x-kubernetes-list-type: atomic
                  tlsCiphers:
                    description: tlsCiphers is the list of TLS ciphers for backend
                      connections.
                    maxLength: 2048
                    type: string
                  tlsContainerRef:
                    description: tlsContainerRef is the reference to the TLS container.
                    maxLength: 1024
                    type: string
                  tlsEnabled:
                    description: tlsEnabled indicates whether backend re-encryption
                      is enabled.
                    type: boolean
                  tlsVersions:
                    description: tlsVersions is the list of TLS versions for backend
                      connections.
                    items:
                      maxLength: 32
                      type: string
                    maxItems: 10
                    type: array
                    x-kubernetes-list-type: atomic
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
