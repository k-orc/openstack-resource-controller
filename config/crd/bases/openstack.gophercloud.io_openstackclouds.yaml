---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: openstackclouds.openstack.gophercloud.io
spec:
  group: openstack.gophercloud.io
  names:
    kind: OpenStackCloud
    listKind: OpenStackCloudList
    plural: openstackclouds
    singular: openstackcloud
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: OpenStackCloud is the Schema for the openstackclouds API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: OpenStackCloudSpec defines the desired state of OpenStackCloud
            properties:
              cloud:
                description: Cloud is the key to look for in the "clouds" object in
                  clouds.yaml.
                type: string
              credentials:
                description: Credentials defines where to find clouds.yaml.
                properties:
                  secretRef:
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  source:
                    type: string
                required:
                - secretRef
                - source
                type: object
            required:
            - cloud
            - credentials
            type: object
          status:
            description: OpenStackCloudStatus defines the observed state of OpenStackCloud
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
