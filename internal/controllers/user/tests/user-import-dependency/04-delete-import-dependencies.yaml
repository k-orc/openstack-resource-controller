---
apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  # We should be able to delete the import dependencies
  - command: kubectl delete domain.openstack.k-orc.cloud user-import-dependency
    namespaced: true
