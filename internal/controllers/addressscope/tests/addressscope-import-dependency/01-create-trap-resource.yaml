---
apiVersion: openstack.k-orc.cloud/v1alpha1
kind: Project
metadata:
  name: addressscope-import-dependency-not-this-one
spec:
  cloudCredentialsRef:
    # TODO(scaffolding): Use openstack-admin if the resource needs admin credentials to be created
    cloudName: openstack
    secretName: openstack-clouds
  managementPolicy: managed
  # TODO(scaffolding): Add the necessary fields to create the resource
  resource: {}
---
# This `addressscope-import-dependency-not-this-one` should not be picked by the import filter
apiVersion: openstack.k-orc.cloud/v1alpha1
kind: AddressScope
metadata:
  name: addressscope-import-dependency-not-this-one
spec:
  cloudCredentialsRef:
    # TODO(scaffolding): Use openstack-admin if the resource needs admin credentials to be created
    cloudName: openstack
    secretName: openstack-clouds
  managementPolicy: managed
  resource:
    projectRef: addressscope-import-dependency-not-this-one
    # TODO(scaffolding): Add the necessary fields to create the resource
