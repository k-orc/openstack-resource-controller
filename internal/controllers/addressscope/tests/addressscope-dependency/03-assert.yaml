---
apiVersion: kuttl.dev/v1beta1
kind: TestAssert
commands:
# Dependencies that were prevented deletion before should now be gone
- script: "! kubectl get project.openstack.k-orc.cloud addressscope-dependency --namespace $NAMESPACE"
  skipLogOutput: true
- script: "! kubectl get secret addressscope-dependency --namespace $NAMESPACE"
  skipLogOutput: true
