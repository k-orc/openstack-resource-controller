---
apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  # We should be able to delete the import dependencies
  - command: kubectl delete user.openstack.k-orc.cloud applicationcredential-import-dependency
    namespaced: true
