---
apiVersion: openstack.k-orc.cloud/v1alpha1
kind: Region
metadata:
  name: region-create-full
status:
  resource:
    name: region-create-full-override
    description: Region from "create full" test
    # TODO(scaffolding): Add all fields the resource supports
  conditions:
  - type: Available
    status: "True"
    reason: Success
  - type: Progressing
    status: "False"
    reason: Success
---
apiVersion: kuttl.dev/v1beta1
kind: TestAssert
resourceRefs:
    - apiVersion: openstack.k-orc.cloud/v1alpha1
      kind: Region
      name: region-create-full
      ref: region
assertAll:
    - celExpr: "region.status.id != ''"
    # TODO(scaffolding): Add more checks
