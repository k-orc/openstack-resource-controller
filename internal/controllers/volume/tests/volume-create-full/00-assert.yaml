
---
apiVersion: openstack.k-orc.cloud/v1alpha1
kind: Volume
metadata:
  name: volume-create-full
status:
  resource:
    name: volume-create-full-override
    description: Volume from "create full" test
    # TODO(scaffolding): Add all fields the resource supports
  conditions:
  - type: Available
    status: "True"
    reason: Success
  - type: Progressing
    status: "False"
    reason: Success
---
apiVersion: kuttl.dev/v1beta1
kind: TestAssert
resourceRefs:
    - apiVersion: openstack.k-orc.cloud/v1alpha1
      kind: Volume
      name: volume-create-full
      ref: volume
    - apiVersion: openstack.k-orc.cloud/v1alpha1
      kind: volumetype
      name: volume-create-full
      ref: volumetype
assertAll:
    - celExpr: "volume.status.id != ''"
    - celExpr: "volume.status.resource.volumetypeID == volumetype.status.id"
    # TODO(scaffolding): Add more checks
