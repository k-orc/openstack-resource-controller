---
apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  # We should be able to delete the import dependencies
  - command: kubectl delete service.openstack.k-orc.cloud endpoint-import-dependency
    namespaced: true
