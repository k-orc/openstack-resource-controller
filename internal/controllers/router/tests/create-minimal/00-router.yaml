apiVersion: openstack.k-orc.cloud/v1alpha1
kind: Router
metadata:
  name: create-minimal
spec:
  cloudCredentialsRef:
    cloudName: openstack
    secretName: openstack-clouds
  managementPolicy: managed
  resource:
    name: create-minimal