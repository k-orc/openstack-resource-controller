---
apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  # We should be able to delete the import dependencies
  - command: kubectl delete loadbalancer lbpool-import-dependency
    namespaced: true
