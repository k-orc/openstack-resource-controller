{{ $packageName := .PackageName -}}
---
apiVersion: openstack.k-orc.cloud/v1alpha1
kind: {{ .Kind }}
metadata:
  name: {{ .PackageName }}-create-full
status:
  resource:
    name: {{ .PackageName }}-create-full-override
    description: {{ .Kind }} from "create full" test
    # TODO(scaffolding): Add all fields the resource supports
  conditions:
  - type: Available
    status: "True"
    reason: Success
  - type: Progressing
    status: "False"
    reason: Success
---
apiVersion: kuttl.dev/v1beta1
kind: TestAssert
resourceRefs:
    - apiVersion: openstack.k-orc.cloud/v1alpha1
      kind: {{ .Kind }}
      name: {{ .PackageName }}-create-full
      ref: {{ .PackageName }}
{{- range .AllCreateDependencies }}
    - apiVersion: openstack.k-orc.cloud/v1alpha1
      kind: {{ . }}
      name: {{ $packageName }}-create-full
      ref: {{ . | camelCase }}
{{- end }}
assertAll:
    - celExpr: "{{ $packageName }}.status.id != ''"
{{- range .AllCreateDependencies }}
    - celExpr: "{{ $packageName }}.status.resource.{{ . | camelCase }}ID == {{ . | camelCase }}.status.id"
{{- end }}
    # TODO(scaffolding): Add more checks
